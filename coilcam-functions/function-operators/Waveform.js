// import {fs} from 'fs';

function waveform(filepath, nbPoints, offset, heightRange, widthRange){
    const values = [];
    fs.readFile(path, (err, data) => {
        let wave = new Uint8Array(data).buffer; //check
        console.log(wave);
    });
}
